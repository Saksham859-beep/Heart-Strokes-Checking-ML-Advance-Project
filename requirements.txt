# Core data processing
numpy>=1.21.0
pandas>=1.3.0

# Machine Learning
scikit-learn>=1.0.0
joblib>=1.1.0

# Visualization (if using the notebook)
matplotlib>=3.4.0
seaborn>=0.11.0

# Web framework
Flask>=2.0.0

# Optional (if using Streamlit for deployment UI)
streamlit>=1.0.0

# For model serialization (pickle is built-in, but joblib is recommended)
# No need to include `pickle` as it’s part of Python's stdlib

# Additional utilities
python-dateutil>=2.8.0
pytz>=2021.1
